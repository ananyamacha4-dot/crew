fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-dotenv>=1.0

# Agents
crewai>=0.80
crewai-tools>=0.20
litellm>=1.50

# LLM provider
openai>=1.30

# Researcher tools (free, no keys required)
duckduckgo-search>=6.0
wikipedia>=1.4
httpx>=0.27

# JSON repair for LLM outputs that emit slightly broken JSON
json-repair>=0.25

# Verifier: headless browser that actually loads the generated HTML and
# reports JS errors. After installing this package you must also run:
#   python -m playwright install chromium
playwright>=1.45
